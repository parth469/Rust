// * main is first function which will run first
fn main() {
    // *  println! calls -> Rust macro.
    // ?  "!" use to identified that we call macro
    // *  ; Use for expression is over
    println!("Hello, world!");
}

// ! Result :-
// * When we run this code we are able to see  Hello, world! print on you screen
